-- issue #610 Q1: cart_promo_codes is now a REGIONAL pivot (see
-- 14-create-cart_promo_codes-regional.sql) — it must NOT live in imartap.
-- OLD-PATH copy of new/18-drop-imartap-cart_promo_codes.sql (identical SQL).
--
-- Drops the legacy imartap copy created by 02-create-cart_promo_codes.sql. Runs
-- AFTER 14 (which created the regional replacement) — so the live table always
-- exists before this drop. The order within old/ guarantees safety; you cannot
-- reach 17 without 14 having created the regional table.
--
-- SAFETY:
--  - cart_promo_codes is TRANSIENT (cleared on every order finalize) — the
--    imartap copy holds at most in-flight carts; no historical data is lost.
--  - Guarded by DATABASE() = 'imartap' → NO-OP against regional DBs (the LIVE
--    regional table is never touched).
--  - DROP TABLE IF EXISTS → idempotent. cart_promo_codes is the FK child
--    (references promo_codes), so dropping it leaves the promo_codes catalog intact.
--
-- NOTE: order_promo_codes is usage HISTORY; its imartap copy is intentionally
-- NOT dropped (see 15) — history is preserved until a per-region backfill runs.
SET @ddl := IF(DATABASE() = 'imartap', 'DROP TABLE IF EXISTS `cart_promo_codes`', 'DO 0');
PREPARE st FROM @ddl; EXECUTE st; DEALLOCATE PREPARE st;
